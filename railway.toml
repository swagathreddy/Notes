[build]
builder = "nixpacks"
buildCommand = "chmod +x build.sh && ./build.sh"

[deploy]
startCommand = "cd backend && gunicorn backend.wsgi"
healthcheckPath = "/"
healthcheckTimeout = 100

# Ensure Python is installed
[env]
PYTHON_VERSION = "3.9"
